{
  "build": {
    "env": {
      "COMPOSER_ALLOW_SUPERUSER": "1"
    },
    "commands": [
      "composer install --no-dev --optimize-autoloader"
    ]
  },
  "start": "php artisan config:clear && php artisan cache:clear && php artisan route:clear && php artisan view:clear && php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=8080"
}




